# String is a immutable data structure 
name = "string" 
nameshort = name[0:3] #this will store the string from index 0 to 3 (excluding 3 )
character = name[1] #it will the single correcter at position 1 .
# If we want to print the charachter from last to we use -ve sign indexing from -1 to -n+1 ; 
